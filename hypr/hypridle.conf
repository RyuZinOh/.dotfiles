general {
  lock_cmd = qs ipc call lockscreen lock
  before_sleep_cmd = loginctl lock-session             # lock before suspend.
  after_sleep_cmd = hyprctl dispatch dpms on           # to avoid having to press a key twice to turn on the display.

  ignore_dbus_inhibit = false                          # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
  ignore_systemd_inhibit = false                       # whether to ignore systemd-inhibit --what=idle inhibitors
}


# listener {
#   timeout = 45                                        #  45sec
#   on-timeout =  brightnessctl -e4 -n2 set 20%-                # set monitor backlight to minimum, avoid 0 on OLED monitor.
#   on-resume = brightnessctl -r                         # monitor backlight restore.
# }

listener {
  timeout = 45                                        # 1min
  on-timeout = loginctl lock-session                   # lock screen when timeout has passed
}

# listener {
#   timeout = 90                                        # 1min30sec
#   on-timeout = hyprctl dispatch dpms off || mmsg -d disable_monitor
#   on-resume = hyprctl dispatch dpms on || mmsg -d enable_monitor
# }
